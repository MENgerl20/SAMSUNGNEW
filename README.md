# Samsung A72 Optimizer (Python/KivyMD)

Это приложение для оптимизации телефона, написанное на Python с использованием библиотеки KivyMD для красивого интерфейса Material Design.

## 📁 Файлы проекта
- `main.py` - Основной код программы.
- `requirements.txt` - Список библиотек, необходимых для работы.

## 🚀 Как запустить на компьютере (для проверки)
1. Установите Python (если не установлен).
2. Откройте терминал/консоль в этой папке.
3. Установите библиотеки:
   ```bash
   pip install -r requirements.txt
   ```
4. Запустите приложение:
   ```bash
   python main.py
   ```

## 🚀 Способ 3: Самый быстрый (GitHub Actions)
Этот способ не требует настройки компьютера или Colab. Вы просто загружаете код на GitHub, и он сам собирает APK.

1.  Создайте репозиторий на GitHub.
2.  Загрузите туда все файлы из папки `SAMSUNGA72` (включая папку `.github`).
3.  Зайдите на GitHub в раздел **Actions**.
4.  Вы увидите запущенный процесс `Build Android APK`.
5.  Подождите 10-15 минут. Когда загорится зеленая галочка, нажмите на него -> внизу будет раздел **Artifacts** -> скачайте файл `package`. Это zip-архив, внутри ваш APK.

## 📱 Способ 2: Google Colab (Если нет GitHub)
Python нельзя просто сохранить как .apk. Его нужно скомпилировать. Для этого используется инструмент **Buildozer**. Он работает **только на Linux** (или WSL на Windows).

### Способ 1: Google Colab (Самый простой, если у вас Windows)
Если вы не хотите устанавливать Linux, используйте Google Colab:
1. Создайте новый ноутбук на [Google Colab](https://colab.research.google.com/).
2. Загрузите туда файлы `main.py` и `requirements.txt`.
3. Выполните следующие команды в ячейках:

   **Ячейка 1 (Установка окружения):**
   Загрузите файл `colab_install.sh` (он есть в папке проекта) в Colab (туда же, где main.py).
   Затем выполните одну короткую команду:
   ```python
   !bash colab_install.sh
   ```

   **Ячейка 2 (Инициализация):**
   Загрузите файл `buildozer.spec` (он есть в папке проекта) в Colab.
   Если вы уже запускали `!buildozer init`, удалите старый `buildozer.spec` и загрузите мой.
   
   **Ячейка 3 (Сборка APK):**
   ```python
   # Очистка старых сборок (на всякий случай)
   !buildozer android clean
   # Запуск сборки с подробным логом
   !buildozer -v android debug
   ```
4. После завершения (минут 15-20), файл `.apk` появится в папке `bin/`. Скачайте его и установите на телефон.

### Способ 2: Если у вас есть Linux/WSL
1. Установите Buildozer: `pip install buildozer`
2. Запустите: `buildozer init`
3. Отредактируйте `buildozer.spec` (добавьте `kivymd` в requirements).
4. Запустите сборку: `buildozer android debug`
